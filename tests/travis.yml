---
- hosts: 127.0.0.1
  connection: local
  vars:
    DEBUG: true
    macos_homebrew:
      packages:
        - { name: "node" }
      casks:
        - { name: "google-chrome" }
  roles:
    - ansible-macos-homebrew